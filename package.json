{
  "name": "ai-tutor",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && node index.js",
    "start": "npm run build && node index.js",
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "preview": "npm run build && node index.js"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "groq-sdk": "^0.15.0",
    "vite": "^6.0.7"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.2"
  }
}
